export function CandidateHomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 text-center">
      <h1 className="text-[clamp(1.4rem,4vw,2rem)] font-semibold">Candidate Area</h1>
      <p className="mt-2 max-w-[32ch] text-slate-600">
        This route is reserved for responsive candidate views.
      </p>
    </main>
  );
}
